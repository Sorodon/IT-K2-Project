#leftright.py
#Functions to move the Brick (or rather its field) left and right.

def left(ymin, xmin, currbrickstate):
	for y in range(1, 5):
		for x in range(1, 5):
			world2[ymin + (
  
 def right(ymin, ymax, currbrickstate):
  
